## Description

<!--
Briefly describe what changed or was added, and link the most relevant files or directories.
Keep this short and concrete. Good examples:
- Added admin import validation in `app/api/admin/import/route.ts` and tests in `tests/app/api/admin/import.test.ts`
- Updated map loading behavior in `components/map/location-picker.tsx`
-->

## Images / Videos

<!--
Attach screenshots, screen recordings, or before/after captures for UI changes.
Helpful examples for this project:
- volunteer report flows
- admin dashboard / admin data pages
- map interactions
- media upload / gallery behavior
- mobile layouts

Omit if no visual change.
-->

## Testing

<!--
List exactly what you tested and how to reproduce it.
Include commands when relevant, for example:
- `npm run lint`
- `npm test`
- manual: sign in as `admin@primap.demo` and verify `/admin/data`
- manual: sign in as `alice@primap.demo` and verify `/report`

Mention any gaps if you did not test something.
-->

## Deploy Notes

<!--
Call out anything needed for deploy or follow-up:
- env vars
- Supabase/schema/data implications
- storage or media migration concerns
- cron/email behavior
- rollback notes

Omit if none
-->

## Reviewer Notes

<!--
Point reviewers to the files or areas that deserve the most attention.
Examples:
- `app/**` for routing, auth, or role-gated behavior
- `components/map/**` for map state and browser-only behavior
- `lib/**` and `lib/supabase/**` for business logic and data access
- `app/api/admin/import/**` and `supabase/**` for import/export or data-shape changes
- `tests/**` for new coverage and edge cases
-->
